# Pinned for reproducible installs. Update deliberately, then re-run the pipeline.
pandas==2.2.3          # data cleaning, scoring, ranking
openpyxl==3.1.5        # read Specter .xlsx exports
python-docx==1.1.2     # generate the Word brief (investor_brief.docx)
anthropic==0.40.0      # optional: --use-llm rationales (requires ANTHROPIC_API_KEY)
